# Authentication
JWT_SECRET=           # Required: 64+ char random hex string. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ISSUER=uk-booking-calendar
JWT_EXPIRY=7d

# Initial admin account (used by seed migration, can be removed after setup)
ADMIN_SEED_EMAIL=admin@example.com
ADMIN_SEED_PASSWORD=changeme
